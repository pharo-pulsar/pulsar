Trait {
	#name : 'DockTSnapshot',
	#category : 'Dock-Browser-Snapshot',
	#package : 'Dock-Browser',
	#tag : 'Snapshot'
}

{ #category : 'accessing - snapshot' }
DockTSnapshot >> restoreFromSnapshot: aSnapshotRecord [

]

{ #category : 'accessing - snapshot' }
DockTSnapshot >> snapshot [

	^ nil
]

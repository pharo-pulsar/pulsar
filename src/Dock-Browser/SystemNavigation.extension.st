Extension { #name : 'SystemNavigation' }

{ #category : '*Dock-Browser' }
SystemNavigation >> codePresenter: aPresenter [
	"This is a stub to provide compatibility. 
	 It should NOT arrive here when using DockBrowser, but in a transition 
	 etape it may happen, so we add this to not crash"
]
